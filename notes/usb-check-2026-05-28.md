# USB Check
签名：codex_ian | 2026-05-28 12:57:00 +10:00 Australia/Sydney

## Result

The phone is connected in Android/LineageOS ADB mode.

Fastboot mode is not active right now.

## ADB Summary

- Device: `lmi`
- Model: `POCO F2 Pro`
- Android version: `16`
- LineageOS version: `23.2-20260422-NIGHTLY-lmi`
- Kernel: `4.19.325-cip128-st12-perf-ga5b3099017ae`
- Kernel build time: `Wed Apr 22 12:22:46 UTC 2026`
- Architecture: `aarch64`
- Verified boot state: `orange`
- USB controller from cmdline: `a600000.dwc3`
- Boot device from cmdline: `1d84000.ufshc`
- Display panel string from cmdline: `qcom,mdss_dsi_j11_38_08_0a_fhd_cmd`
- `dtbo_idx`: `7`
- `dtb_idx`: `0`
- Hardware certificate/model marker: `M2001J11E`

Private identifiers such as serial number and CPU ID are present in
`logs/phone-adb.txt`; do not share the raw log without redaction.

## Important Partitions Observed

```text
boot           -> /dev/block/sde50
dtbo           -> /dev/block/sde47
recovery       -> /dev/block/sda28
super          -> /dev/block/sda32
userdata       -> /dev/block/sda34
metadata       -> /dev/block/sda18
persist        -> /dev/block/sda22
persistbak     -> /dev/block/sda23
vbmeta         -> /dev/block/sde16
vbmeta_system  -> /dev/block/sde17
vbmeta_vendor  -> /dev/block/sde25
vbmeta_product -> /dev/block/sde26
vbmeta_odm     -> /dev/block/sde27
```

No `vendor_boot` or `init_boot` partition was observed in the filtered partition
check.

## Script Fix

`scripts/03_check_phone_usb.bat` was corrected so it does not run
`fastboot getvar all` unless `fastboot devices` actually reports a fastboot
device. This prevents the script from hanging when the phone is in ADB mode.

## Next Safe Step

Use the ADB data above to update the lmi `deviceinfo` and kernel package audit,
but do not flash anything yet. The current local `linux-xiaomi-lmi` package
still needs the kernel repository, commit, and config to be identified.
